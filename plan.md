# 中国政区拼图：开发与发布计划

## 目标与角色

当前交付教师教学版（teaching），用于公开课展示、接力拼图、方位与邻省复习。后续testing面向学生练习或测验，先保留分支起点，功能规则确认后开发。main保存稳定基线。

## 已实现范围

- 34个省级行政区的独立轮廓与完成状态。
- 使用本地保存的地图源数据生成SVG，南海诸岛及界线作附图，港澳提供放大定位。
- 鼠标/触屏拖拽、点击配对、正确吸附、错放重试、去重计数。
- 提示、计时、通关弹窗、重玩、响应式布局。
- 自动类型与状态检查；原Sites构建和新增独立静态构建。

## 从开发到发布的阶段

| 阶段 | 输入与工作 | 输出 | 验收条件 |
| --- | --- | --- | --- |
| 需求设计 | 明确教师/学生角色、34区与南海要求 | 本计划、README | 区分现有与未来功能 |
| 数据准备 | 仓库内原始GeoJSON、生成脚本 | lib/map-data.json | 34区数量/名单/坐标范围正确 |
| 页面开发 | 同一React游戏组件、CSS、SVG | 教师教学版 | 拖拽/提示/计数状态完整 |
| 本地检查 | npm run check、build:web | 无类型错误、有效静态产物 | 状态检查通过 |
| Git版本管理 | main、teaching、testing | GitHub私有源码与历史 | 分支可查看、构建可追溯 |
| 自动检查 | push或PR触发CI | 每个分支的构建产物 | Actions成功 |
| 课堂验收 | 实际电脑、浏览器、触屏、网络 | 实操反馈 | 尚待现场验证 |
| 网页发布 | 选定分支与已验证产物 | Pages或学校服务器URL | 目标用户可打开，资源无缺失 |
| 维护回退 | 提交号、版本记录、旧产物 | 可重建和回退的版本 | 不用强推删历史 |

## 发布策略

1. GitHub用于代码、分支和构建产物，默认私有。
2. 原Sites私有演示保留；新增静态构建解除对Sites登录及Worker运行时的依赖。
3. GitHub Pages提供手动工作流，尚未启用；公开入口/套餐/学校网络仍需确认。
4. 学校服务器可接收dist-web全部文件；教师版与学生版分别放置，后续再配置具体地址。
5. 本地HTTP版本作为不依赖外网的投屏备用。部署操作步骤见README第8—13节。

## 后续学生版需要确认的规则

- 是自由练习还是限时测验？
- 是否隐藏省名、提示或底板信息？
- 错放、提示、用时如何影响得分？
- 是否需要成绩导出？是否真的需要账号与在线成绩存储？
- 教师和学生两个入口如何同时部署？

这些内容当前未实现。先确定规则，再在testing分支开发和验证，确认后发布学生入口，避免改变已用于公开课的teaching版本。

## 当前验证边界

自动检查不替代地图专业审校、浏览器拖拽实测或大陆校园网络实测。完整通关弹窗、触屏和学校访问应在正式上课前验收。详细README是操作手册，本文件负责范围、阶段和验收点。
