# 教师教学版：项目文件说明

## 1. 打开方式

- 在线：[教师教学版](https://liboran2024.github.io/china-province-puzzle-teaching/)，无需Python和ChatGPT账号。
- 本机：到 `E:\拼图\teaching` 双击 `启动教师版.bat`；启动窗口需保持运行。本机已有Python 3，默认地址为 `http://127.0.0.1:8765/`，端口被占用时使用窗口打印的新地址。
- 复制到别的电脑：将“教师教学版-本地网页.zip”完整解压，再双击其中 `启动游戏.bat`。本地启动依赖Python 3；没有Python时使用在线入口。
- 不要双击 `index.html`，模块脚本应通过HTTP打开。

## 2. 文件夹对应什么

| 位置 | 用途 | 是否需要手动修改 |
| --- | --- | --- |
| `project/` | 完整源码工程和Git历史 | 开发时修改 |
| `local/` | 已构建网页：HTML、assets、地图数据、启动脚本 | 使用时不修改；构建后整体更新 |
| `archive/` | 整理前散落的解压文件、旧说明、旧打包暂存文件 | 仅作备份 |
| `启动教师版.bat` | 启动local中的Python HTTP服务并打开浏览器 | 双击使用 |
| `打开在线教师版.url` | GitHub Pages入口快捷方式 | 双击使用 |
| `教师教学版-本地网页.zip` | 可分发的本地网页包 | 解压使用 |

本次目录整理没有更改教师版游戏规则、页面组件或已发布网页。其功能仍是34省级行政区、带内界地图、具名碎片、提示、吸附、计时与通关庆祝。

## 3. 源码重点文件

| project内路径 | 说明 |
| --- | --- |
| `app/page.tsx` | 教师游戏组件、拖拽、提示、计时、弹窗 |
| `app/globals.css` | 页面布局、配色、响应式与动画 |
| `lib/game.ts` | 去重、完成判定、计时格式 |
| `lib/map-data.json` | 生成后的34区轮廓与南海附图 |
| `public/china-source.json` | 原始GeoJSON，更新地图时修改这一来源 |
| `scripts/prepare-map.mjs` | 由原始数据生成游戏轮廓 |
| `scripts/check-game.mjs` | 数量、边界范围、错放、去重和通关检查 |
| `web/main.tsx`、`index.html` | 独立静态网页入口 |
| `vite.web.config.ts` | 构建静态网页到dist-web |
| `start_game.py`、`启动游戏.bat` | 本地HTTP启动工具 |
| `.github/workflows/pages.yml` | GitHub Pages手动发布 |
| `.github/workflows/check.yml` | GitHub自动检查和构建 |
| `.git/` | Git版本历史；不要手动编辑或清空 |
| `node_modules/` | npm依赖；不需复制给使用游戏的教师 |
| `dist-web/` | 自动生成的静态产物；不作为源码编辑 |
| `.openai/`、`vite.config.ts` | 原Sites兼容配置；日常GitHub发布使用web构建 |

## 4. 开发到发布

```powershell
cd E:\拼图\teaching\project
git switch teaching
npm ci
npm run dev:web
```

修改后运行 `npm run check` 与 `npm run build:web`。将 `dist-web/` 完整复制到 `../local/`，并复制 `start_game.py`、`启动游戏.bat`；打包local内容用于分发。

GitHub仓库为 [china-province-puzzle-teaching](https://github.com/liboran2024/china-province-puzzle-teaching)，默认分支 `teaching`。提交并推送后，到Actions手动运行Pages发布工作流，选择 `teaching`；普通提交只检查，不自动覆盖线上版。

学生测验版现为独立目录 `E:\拼图\testing` 和独立仓库 `china-province-puzzle-testing`，不再用教师仓库旧的testing起始分支部署。两个站点互不覆盖。

## 5. 注意

地图为教学互动示意，源于DataV.GeoAtlas。当前不存储学生账号或成绩。学校网络与触屏使用请在正式上课前实测，保留本地包作为备用。

整理日期：2026-09-06。
